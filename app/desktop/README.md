# Desktop workspace

Tauri hosts the connected viewer and bundled local server. From the repository root:

```sh
pnpm desktop:dev
pnpm desktop:build
```

The [desktop runtime guide](../../docs/desktop.md) owns prerequisites, build outputs,
process lifecycle, local installation and signed-update requirements. Follow the
[development guide](../../docs/development.md) for verification and separate service
or plugin updates.
